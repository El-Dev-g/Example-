
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow admins to read and write to the users collection.
    // In a production app, you would want to add more granular role-based access control.
    match /users/{userId} {
      allow read, write: if request.auth != null;
    }
  }
}
